apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edge-processor.fullname" . }}-test-connection"
  labels:
    {{- include "edge-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edge-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
